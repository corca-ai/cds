import { ReactNode } from 'react';
export interface TheadProps {
    children: ReactNode;
    height?: number;
}
export declare const Thead: ({ children }: TheadProps) => import("@emotion/react/jsx-runtime").JSX.Element;
